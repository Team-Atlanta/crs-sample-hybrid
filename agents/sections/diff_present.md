## Diff (Delta Mode)

Vulnerabilities were introduced by a code change. The following diff files show what changed:

{diff_list}

The harness is built with this diff applied — the vulnerabilities exist in the current code.
Focus on code paths affected by the diff. Look for bugs detectable by the sanitizer or security hooks.
