## Seed Corpus

Seed inputs are available in this directory for understanding the expected input format:

{seed_dir}

Inspect the files under this directory as needed to understand the input structure. Mutate or craft new inputs based on these seeds to trigger vulnerabilities. (The fuzzer is already mutating these seeds — your edge is reasoning about the source to reach paths the fuzzer struggles with.)
